Данный код определяет класс Hotel, который представляет собой модель для отелей, 
позволяющую инициализировать их с определенной ценой и названием. 
Также в классе определен метод fill для случайного заполнения объекта Hotel данными из заданных списков цен и
названий отелей.

Класс Hotel используется для создания объектов, представляющих отели. 
Он имеет конструктор и метод для заполнения его свойств случайными данными.

Атрибуты:
    price: Хранит цену проживания в отеле (по умолчанию None).
    name: Хранит название отеля (по умолчанию None).
Методы:
    __init__(self, price=None, name=None)
        Конструктор класса Hotel.

Аргументы:
    price: (опционально) цена проживания в отеле.
    name: (опционально) название отеля.
Описание:
    При инициализации создается объект Hotel, который может иметь заданные цену и название. 
    Если они не указаны, соответствующие атрибуты будут равны None.

fill(self)
    Метод для создания и возвращения нового экземпляра класса Hotel с случайно сгенерированными ценой и
    названием.

Аргументы:
    Нет.
Возвращает:
    Новый объект Hotel, инициализированный случайным образом:
    price: Случайно выбранное значение из списка HOTEL_PRICES.
    name: Случайно выбранное значение из списка HOTEL_NAMES.
Описание:
    Метод использует функцию choice из модуля random для выбора случайного значения из списков, 
    предварительно загруженных в config. Это позволяет динамически генерировать данные отелей, 
    что может быть полезно для симуляции или тестирования.

Пример использования
    # Создание нового отеля с заданными значениями
    hotel1 = Hotel(price=150, name='Hotel California')

    # Случайное заполнение нового отеля
    random_hotel = hotel1.fill()
    print(random_hotel.price, random_hotel.name)

    В приведенном примере создается объект Hotel с заданной ценой и названием, 
    после чего происходит случайное заполнение нового объекта отеля.